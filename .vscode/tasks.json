{
    "tasks": [
        {
            "label": "gtest",
            "type": "shell",
            "command": "${workspaceFolder}/build/test/test_runner",
            "problemMatcher": []
        }
    ]
}
